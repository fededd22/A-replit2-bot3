# تشغيل المشروع على Replit

تمت إضافة ملف `.replit` جاهز (يضبط Node.js 22 + تثبيت `curl` و`unzip` اللازمين لتنزيل ثنائي V2Ray تلقائيًا عند أول تشغيل، وضبط منفذ واحد فقط يُعرض للخارج لأن كل الاتصالات — بما فيها WebSocket الخاص بـ VLESS/VMess/Trojan — تمر عبر نفس خادم Express على نفس المنفذ).

## خطوات الرفع
1. على Replit: **Create Repl → Import from GitHub** (الأسهل) بعد رفع هذا المجلد إلى مستودع GitHub، أو أنشئ Repl فارغ من نوع **Node.js** واسحب/أفلت كل هذه الملفات داخله (أو استخدم زر Upload folder).
2. من قائمة **Tools → Secrets** أضف المتغيرات التالية (لا تضعها في `.env` مباشرة حتى لا تُرفع بالخطأ لمستودع عام):
   - `TELEGRAM_BOT_TOKEN`
   - `MURAD_SETUP_PASSWORD` (مهم: غيّرها، القيمة الافتراضية `moon2026` موجودة في الكود ويعرفها أي شخص يطّلع على المستودع)
   - `APP_URL` — بعد أول تشغيل، انسخ رابط الـ Repl (يظهر في نافذة Webview، بصيغة `https://xxxx.repl.co` أو رابط الـ Deployment) وضعه هنا
   - (اختياري) `DATA_DIR` إذا أردت تخزين البيانات في مسار مختلف
3. اضغط **Run**. أول تشغيل سيحمّل ثنائي V2Ray تلقائيًا من GitHub (يستغرق بضع ثوانٍ).

## ملاحظات مهمة
- **النشر الدائم:** تشغيل الـ Repl العادي يتوقف عند الخمول. للحصول على خادم يعمل باستمرار (ضروري لبوت VPN)، استخدم **Deployments → Reserved VM** وليس Autoscale، لأن Autoscale يوقف الـ instances عند عدم الاستخدام ونظام الملفات فيه غير دائم (سيفقد بيانات العملاء `clients.json` بين الطلبات).
- **الأمان:** يحتوي `server.ts` على توكن تلقرام افتراضي مكتوب مباشرة داخل الكود (سطر ~66) ككل احتياطي. بمجرد ضبط `TELEGRAM_BOT_TOKEN` في Secrets سيُستخدم توكنك بدلاً منه، لكن يُفضّل حذف القيمة الافتراضية من الكود إن كان المستودع عامًا.
- منافذ V2Ray الداخلية (10080-10092) لا تحتاج لعرضها خارجيًا على Replit؛ خادم Express يستقبل كل شيء عبر WebSocket على المنفذ الوحيد المعروض ويمرره داخليًا.
